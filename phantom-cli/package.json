{
  "name": "phantom-cli",
  "version": "1.0.0",
  "module": "src/main.ts",
  "type": "module",
  "bin": {
    "phantom": "./src/main.ts"
  },
  "scripts": {
    "install-bin": "rm -rf dist && bun build src/main.ts --compile --outfile dist/phantom && mv dist/phantom ~/.local/bin/phantom"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
