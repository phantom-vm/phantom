{
  "name": "phantom-cli",
  "version": "1.0.0",
  "module": "src/main.ts",
  "type": "module",
  "bin": {
    "phantom": "./src/main.ts"
  },
  "scripts": {
    "install-bin": "rm -rf dist && bun build src/main.ts --compile --minify --outfile dist/phantom && mv dist/phantom ~/.local/bin/phantom",
    "build-user-bin": "rm -rf dist && bun build src/main-user.ts --compile --minify --outfile dist/phantom"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14"
  },
  "peerDependencies": {
    "typescript": "^7.0.2"
  }
}
